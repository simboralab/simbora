# ============================================
# ARQUIVO .env - VARIÁVEIS DE AMBIENTE
# ============================================
# 
# IMPORTANTE: Este arquivo é OPCIONAL com Dynaconf
# O Dynaconf prioriza:
# 1. Variáveis de ambiente com prefixo SIMBORA_*
# 2. Arquivo .secrets.toml (recomendado para secrets)
# 3. Arquivo settings.toml
# 4. Este arquivo .env (se existir)
#
# Para usar: copie este arquivo para .env na raiz do projeto
# cp .env.example .env

# ============================================
# AMBIENTE
# ============================================
# Define qual ambiente usar: development, production, testing
SIMBORA_ENV=development

# ============================================
# SECRETS (OPCIONAL - Use .secrets.toml se possível)
# ============================================
# Se preferir usar .env em vez de .secrets.toml, descomente e preencha:
# SIMBORA_SECRET_KEY=sua-chave-secreta-aqui
# SIMBORA_FIELD_ENCRYPTION_KEY=sua-chave-fernet-aqui

# ============================================
# CONFIGURAÇÕES DJANGO (OPCIONAL)
# ============================================
# Essas configurações podem sobrescrever o settings.toml
# SIMBORA_DEBUG=True
# SIMBORA_ALLOWED_HOSTS=["127.0.0.1", "localhost"]

# ============================================
# EMAIL (OPCIONAL)
# ============================================
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# EMAIL_HOST=localhost
# EMAIL_PORT=25
# EMAIL_USE_TLS=False
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=
